import { RoutePage } from "@/app/_shared/RoutePage";
export default function Reconciliation(){return <RoutePage slug="features" title="Reconciliation with the full picture." description="Match records across providers and settlement files while preserving the reason behind every outcome."/>}
