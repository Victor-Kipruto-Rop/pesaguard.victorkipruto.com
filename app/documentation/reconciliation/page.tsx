import { DocsPage } from "@/app/_shared/DocsPage";

export default function Reconciliation() {
  return (
    <DocsPage title="Reconciliation" intro="There is no public reconciliation endpoint yet.">
      <section>
        <p>Reconciliation results are reviewed in the PesaGuard dashboard rather than through the API.</p>
      </section>
    </DocsPage>
  );
}
