import { RoutePage } from "@/app/_shared/RoutePage";
export default function TenantIsolation(){return <RoutePage slug="security" title="Strong boundaries between tenants." description="Keep customer data scoped to the organization and context that owns it."/>}
