import { RoutePage } from "@/app/_shared/RoutePage";
export default function Authentication(){return <RoutePage slug="security" title="Authentication teams can reason about." description="Use explicit identity and secure service boundaries across the platform."/>}
