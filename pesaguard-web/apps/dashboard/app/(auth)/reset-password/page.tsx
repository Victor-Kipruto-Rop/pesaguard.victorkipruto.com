import { AuthPendingPage } from "@/components/auth/AuthPendingPage";
export default function ResetPasswordPage() { return <AuthPendingPage title="Set a new password" detail="A valid server-issued reset token is required to continue." />; }
